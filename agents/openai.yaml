display_name: Code Semantic Sync
short_description: Use codetalker to maintain semantic maps for AI code changes
default_prompt: Use $code-semantic-sync. Read the semantic map before editing, use codetalker commands for config/map/ask/plan/sync/check workflows, and sync the map after edits.
